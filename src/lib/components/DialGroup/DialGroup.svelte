<script lang="ts">
    import type { Snippet } from "svelte";
    let {
        class: className = "",
        label = undefined,
        children,
        ...restProps
    }: {
        label?: string;
        children: Snippet;
        [key: string]: unknown;
    } = $props();
</script>

<!-- DialGroup.svelte -->
<div class={`dial-group ${className}`} aria-label={label} {...restProps}>
    {@render children()}
</div>

<style>
    .dial-group {
        display: block;
    }
</style>
