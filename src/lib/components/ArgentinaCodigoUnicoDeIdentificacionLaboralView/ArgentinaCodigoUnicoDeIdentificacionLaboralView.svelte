<script lang="ts">
    import type { Snippet } from "svelte";
    let {
        class: className = "",
        label = undefined,
        children,
        ...restProps
    }: {
        label?: string;
        children: Snippet;
        [key: string]: unknown;
    } = $props();
</script>

<!-- ArgentinaCodigoUnicoDeIdentificacionLaboralView.svelte -- generated by bin/new-component; replace <div> with the real semantic element -->
<div class={`argentina-codigo-unico-de-identificacion-laboral-view ${className}`} aria-label={label} {...restProps}>
    {@render children?.()}
</div>
