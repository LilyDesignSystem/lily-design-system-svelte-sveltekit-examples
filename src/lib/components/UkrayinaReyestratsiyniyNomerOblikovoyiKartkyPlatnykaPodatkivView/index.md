# UkrayinaReyestratsiyniyNomerOblikovoyiKartkyPlatnykaPodatkivView

UkrayinaReyestratsiyniyNomerOblikovoyiKartkyPlatnykaPodatkivView is a headless placeholder component, generated by `bin/new-component` (plan P7-T3). a read-only display of Ukraine's Taxpayer Registration Card Number (RNOKPP)

**Status:** experimental — generated by bin/new-component; not yet reviewed, styled, or exercised in a composed flow.

## Implementation Notes

- Renders a generic `<div>` root with the kebab-case base class `ukrayina-reyestratsiyniy-nomer-oblikovoyi-kartky-platnyka-podatkiv-view` plus the consumer's class hook
- Accepts `label` to set `aria-label`
- Spreads rest props / additional attributes onto the root element
- Children / default slot renders the component's content

## Props

- `className`: string (default: `""`) -- CSS class name appended to the base class
- `label`: string (optional) -- sets `aria-label` on the root
- `children`: slot (required) -- the component's content
- `...restProps`: unknown -- additional attributes spread onto the root element

## Usage

```html
<UkrayinaReyestratsiyniyNomerOblikovoyiKartkyPlatnykaPodatkivView label="UkrayinaReyestratsiyniyNomerOblikovoyiKartkyPlatnykaPodatkivView example">Content</UkrayinaReyestratsiyniyNomerOblikovoyiKartkyPlatnykaPodatkivView>
```

## Keyboard Interactions

None yet.

## ARIA

`aria-label` when `label` is provided.

## When to Use

- This is a generated scaffold — replace this section once the component's real purpose is decided.

## When Not to Use

- This is a generated scaffold — replace this section once the component's real purpose is decided.

## Headless

This headless component renders a generic `<div>`. Replace it with the correct semantic element before shipping.

## Styles

The consumer provides all CSS styling via the `.ukrayina-reyestratsiyniy-nomer-oblikovoyi-kartky-platnyka-podatkiv-view` class.

## Testing

- Verify the component renders its root element with class `ukrayina-reyestratsiyniy-nomer-oblikovoyi-kartky-platnyka-podatkiv-view`
- Verify children content is rendered
- Verify `label` sets `aria-label`
- Verify pass-through attributes are applied

## References

- AGENTS/headless.md

---

Lily™ and Lily Design System™ are trademarks.
