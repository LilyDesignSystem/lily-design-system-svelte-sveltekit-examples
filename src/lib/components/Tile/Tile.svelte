<script lang="ts">
    import type { Snippet } from "svelte";
    let {
        class: className = "",
        label = undefined,
        children,
        ...restProps
    }: {
        label?: string;
        children: Snippet;
        [key: string]: unknown;
    } = $props();
</script>

<!-- Tile.svelte -->
<div class={`tile ${className}`} aria-label={label} {...restProps}>
    {@render children()}
</div>

<style>
    .tile {
        display: block;
    }
</style>
