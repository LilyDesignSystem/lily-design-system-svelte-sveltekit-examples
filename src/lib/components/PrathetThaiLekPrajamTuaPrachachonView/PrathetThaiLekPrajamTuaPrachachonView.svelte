<script lang="ts">
    import type { Snippet } from "svelte";
    let {
        class: className = "",
        label = undefined,
        children,
        ...restProps
    }: {
        label?: string;
        children: Snippet;
        [key: string]: unknown;
    } = $props();
</script>

<!-- PrathetThaiLekPrajamTuaPrachachonView.svelte -- generated by bin/new-component; replace <div> with the real semantic element -->
<div class={`prathet-thai-lek-prajam-tua-prachachon-view ${className}`} aria-label={label} {...restProps}>
    {@render children?.()}
</div>
