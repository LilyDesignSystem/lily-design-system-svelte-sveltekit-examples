<script lang="ts">
    import type { Snippet } from "svelte";
    let {
        class: className = "",
        label = undefined,
        children,
        ...restProps
    }: {
        label?: string;
        children: Snippet;
        [key: string]: unknown;
    } = $props();
</script>

<!-- TableRow.svelte -->
<div class={`table-row ${className}`} aria-label={label} {...restProps}>
    {@render children()}
</div>

<style>
    .table-row {
        display: block;
    }
</style>
